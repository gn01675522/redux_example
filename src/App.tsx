import { lazy, Suspense } from "react";

import type { FC } from "react";

const App: FC = () => {
  return (
    <div className="App">
      <Suspense></Suspense>
    </div>
  );
};

export default App;
